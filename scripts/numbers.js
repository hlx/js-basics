const num1 = 100;
const num2 = 60;
let val;

val = num1 + num2;
val = num1 - num2;
val = num1 / num2;
val = num1 * num2;
val = num1 % num2;

val = Math.PI;
val = Math.round(Math.PI);
val = Math.ceil(Math.PI);
val = Math.floor(Math.random() * 20 + 1); // random number 1-20

console.log(val);